# Reference only. Runtime env files live in `api/` and `web/`.

# API
DATABASE_URL=mysql://app:app@127.0.0.1:3306/full_stack_base
PORT=3000
CORS_ORIGIN=http://localhost:5173

# Web
VITE_API_BASE_URL=/api
